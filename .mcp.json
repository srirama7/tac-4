{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp",
        "--isolated",
        "--headless",
        "--viewport-size",
        "1920,1080"
      ]
    }
  }
}
